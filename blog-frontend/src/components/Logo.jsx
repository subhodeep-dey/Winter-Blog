import React from 'react';
import logo from '../assets/logo.jpg';

const Logo = () => {
    return (
        <div className="logo">
            <img src={logo} alt="Logo" />
        </div>
    );
};

export default Logo;